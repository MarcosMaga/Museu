<!DOCTYPE html>
<html lang="pt-br">

<head>
	<meta http-equiv="content-type" content="text/html; charset=UTF-8">
	<meta charset="utf-8">
	<title>Formulário de Inclusão de Obra de Arte</title>
</head>

<body>
	<h1><%= titulo %></h1>
	<section>
		<% if(erros) { %>
			<ul>
				<% for(let i=0; i < erros.length; i++) { %>
					<li>
						<%=erros[i].msg%>
					</li>
				<% } %>
			</ul>
		<% } %>
	</section>
	<form action="<%= data.hasOwnProperty('idobra') ? `/update/core/${data.idobra}` : '/create/core'%>" method="post">
		<input type="text" name="nome" placeholder="Digite o nome da obra" value="<%= data != null ? data.nome : '' %>">
		<input type="number" name="ano" placeholder="Digite o ano da obra" value="<%= data != null ? data.ano : '' %>">
		<input type="text" name="artista" placeholder="Digite o nome da autora ou autor da obra" value="<%= data != null ? data.artista : '' %>">
		<input type="text" name="urlimagem" placeholder="Digite a url da foto da imagem" value="<%= data != null ? data.urlimagem : '' %>">
		<input type="text" name="descricao" placeholder="Digite a descrição da obra" value="<%= data != null ? data.descricao : '' %>">
		<input type="submit" value="enviar">
	</form>
</body>

</html>